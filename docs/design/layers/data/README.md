# Data layer

The **data layer** is the part of `mono-control` concerned with persisted state
and access to it.

It comprises one or more **data sources** — concrete origins of data within the
layer. There isn't much more to say at this level: it's the data layer. The
substance lives in the individual sources.

## Data sources

Each data source is documented in its own file here:

- [config-source.md](config-source.md)
