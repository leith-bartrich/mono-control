"""mono-control engines — the verbs that act on the data nouns.

Each engine consumes the data layer's nouns and drives the git layer.

- ``source`` — brings repos into local availability (clone / init / fetch).
- ``layout`` — arranges the local layout (place / checkout / relocate / retire).
- ``publish`` — reserved outbound stub (no implementation yet).
"""
